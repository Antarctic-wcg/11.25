### 2. 请写出块级元素,行内元素,行内块元素有何异同, 分别包括那些常用的标签
```
块级元素: 一行只能放一个,宽度为父元素的宽度,高度根据内容定,默认样式display:block.有div,h1-h6,p,dd,dt,dl,ul,li,table,tr,td
行内元素: 一行可以放多个,宽高由内容觉得,不可以设置宽高.有i,b,br,span,
行内块元素: 一行可以有多个,可以设置宽高.有a,img,input
```
### 3.盒子的宽高怎么计算
```
宽: border-left + border-right + padding-left + padding-right + content
高: border-top + border-bottom + padding-top + padding-bottom + content
```
### 4.确认颜色
```
这不是内容吗?( green  色)
试问这行字体是什么颜色的?( blue  色)
```
### 5. 清除浮动的四种方式
```
1.直接给父盒子设置一个高度(严格的说不算清除浮动)。这种方式简单方便，但是当父盒子没有办法直接设置高度就必须用其他方法清除浮动。
2.在发生浮动的父盒子中的最后添加一个空元素，然后给该空元素设置clear属性值。
3.直接给父盒子设置overflow属性
4.使用伪元素清除浮动css .clearfix::after, .clearfix::before { content: ""; height: 0; display: block; visibility: hidden; line-height: 0; clear: both; } .clearfix { zoom: 1;//兼容ie }
```
### 6. 解释下绝对定位的位置怎么确定
```
绝对定位的位置是相对自己的上一级设置了定位的元素,如果自己的上一次没有设置定位,那么就相对body定位
```
### 8. margin在使用中的特殊情况
```
(1)当一个元素出现在另一个元素上面时，第一个元素的下外边距与第二个元素的上外边距会发生合并。都为正的时候取最大值的外边距.
(2)当一个元素包含在另一个元素中时（假设没有内边距或边框把外边距分隔开），它们的上和/或下外边距也会发生合并。都为正时取最大的值有效
(3)假设有一个空元素，它有外边距，但是没有边框或填充。在这种情况下，上外边距与下外边距就碰到了一起，它们会发生合并.如果这个外边距遇到另一个元素的外边距，它还会发生合并
```
### 9. img标签有哪些常用的属性,各代表什么意思
```
src:图像标签的图片地址
alt:图片说明
width: 图片宽度
height: 图片高度
id: 标签id值
class: 标签类名
name: 和id差不多,用在form中
```
### 10. 解释下常用的伪类选择器,分别代表什么意思
```
:hover用于当用户把鼠标移动到元素上面时的效果；
:active用于用户点击元素那一下的效果
:focus用于元素成为焦点，这个经常用在表单元素上。
:first-child选择某个元素的第一个子元素；
:last-child选择某个元素的最后一个子元素；
:nth-child()选择某个元素的一个或多个特定的子元素；
:nth-last-child()选择某个元素的一个或多个特定的子元素，从这个元素的最后一个子元素开始算；
:nth-of-type()选择指定的元素；
:nth-last-of-type()选择指定的元素，从元素的最后一个开始计算；
:first-of-type选择一个上级元素下的第一个同类子元素；
:last-of-type选择一个上级元素的最后一个同类子元素；
:only-child选择的元素是它的父元素的唯一一个了元素；
:only-of-type选择一个元素是它的上级元素的唯一一个相同类型的子元素；
:empty选择的元素里面没有任何内容。

```
